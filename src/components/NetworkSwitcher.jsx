"use client";

import React from 'react';

/**
 * Network Switcher Component
 * Note: For Sui-based chains like One Chain, network switching is handled
 * automatically by the wallet (OneWallet, Sui Wallet, etc.)
 * This component is kept for compatibility but doesn't render anything
 */
const NetworkSwitcher = () => {
  // Sui wallets handle network selection automatically
  // No need for manual network switching UI
  return null;
};

export default NetworkSwitcher;